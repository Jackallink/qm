# Checkpoint — v0.2.0 (2026-08-16)

Branch: `yh/qm-integration` @ `5b855f2` (tag `v0.2.0`, pushed to private)
Previous baseline: `main` @ v0.1.4 (151 commits behind)

## Released scope

- **Remote Turn (F1)** — Gate 1–3 complete; live e2e (real Docker +
  Postgres + deepseek) full lifecycle; deploy/rotate/rollback drill
  executed; D0-T coverage table closed (agent-model).
- **Engines** — pi / opencode / codex / claude / prime / hermes / mock
  (7 first-class); prime + hermes re-enabled with real e2e; claw
  quarantined (F0), OpenClaw gateway integration = separate program.
- **Security** — approval bridge, budget metering, soul, sandbox
  isolation, network-layer forced egress (TLS transparent redirection),
  security-path tests.
- **Tests** — core 214 + layer 21 + local-sandbox 17 + typecheck 0.

## Branch plan

New branch: `yh/agent-management` (from this checkpoint).

Goal: re-enable / rebuild the Agent management layer currently quarantined
by F0 — Agent Registry (registration/lifecycle), SOP/gate engine,
Messenger (agent messaging), Agent Scheduler — as first-class, verified
capabilities (same discipline as prime/hermes re-enable: wiring +
config + tests + live e2e, fresh-context review).

Note: this is the layer the 标书 Phase 3 items (PC Agent, OpenClaw 纳管,
management dashboards) build on. Keep the checkpoint as the rollback
point: `git checkout v0.2.0`.

## Restore notes

- Test PG container (`qm-test-pg-tmp`) may need `docker start` after host
  disk pressure.
- Sandbox image `qm-sandbox-prime:latest` rebuilt with prime-agent 0.7.2;
  rebuild via `scripts/sandbox-local-build-with-prime.sh`.
- egress TLS material in `/tmp/qm-egress-tls` (regenerate via
  `plugins/egress-gw/src/tls-material.ts` if lost).

## Agent management branch notes (2026-08-16)

Re-enabled Registry/SOP/Messenger/Scheduler (wiring + routes + PG maps +
f0 updates + unit tests). Route-table assertions and Fastify inject tests
all pass (24 agent routes registered; emergency/dashboard still absent).

KNOWN DEV-ENVIRONMENT ISSUE: the dev-instance HTTP surface returns 404 for
the newly registered `:param` agent routes (`/v1/admin/workspaces/:ws/...`,
`/v1/admin/agents/:id/...`) while Fastify inject and route-table tests
pass 200. Root cause not yet found; not a code defect (inject proves the
routes match). Investigation backlog: compare dev-instance core boot vs
test server (gate hooks, Fastify version, --watch cache after branch
switch). Templates (no param) and existing `:param` admin routes (e.g.
`/v1/admin/scopes/:scope`) work on dev, so it is specific to the newly
added parameterized agent routes.
