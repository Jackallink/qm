# Checkpoint — v0.2.1 (2026-08-16)

Branch: `yh/agent-management` @ `da9cbcc` (pushed to private)
Previous baseline: `v0.2.0` (`yh/qm-integration`)

## Released scope

- **Agent management layer re-enabled** — Agent Registry
  (registration/lifecycle), SOP/gate engine, Messenger (agent
  messaging), Agent Scheduler. All four wired with PG-durable stores
  (`agents` / `sop_runs` / `agent_messages` / `agent_subscriptions` /
  `scheduled_jobs` tables), admin-gated routes, f0 inventory updated.
- **Live e2e verified** — create/list/update agent, SOP sign
  gate/rollback/history, messenger send/inbox/ack, scheduler job
  create — full lifecycle on the dev instance (201/200 paths).
- **Fresh-context review closed** — 3 blockers (missing
  authorizeAdmin on messenger + health handlers; stop/delete state
  machine 500 + process leak), 4 majors (shell injection in stopAgent;
  decorative HMAC; registration gate mutating persisted manifest;
  non-atomic SOP transitions), 3 minors, test blind spots — all fixed
  with regression tests.
- **Tests** — core 239 + layer 21 + local-sandbox 17 + typecheck 0.

## Explicitly not in scope (still quarantined)

- **Emergency / dashboard** — handlers are audit-only stubs that
  report success without performing the claimed action; per the
  honest-success rule they stay quarantined until they perform and
  prove the action (real circuit break, real session termination,
  real dashboards over audit_log). Planning item only.
- **Claw / OpenClaw gateway** — separate multi-channel gateway
  program (Phase 3).

## Restore / rollback

- Restore point: `git checkout v0.2.1` (tag this checkpoint after
  final review passes).
- Branch plan for next iteration: emergency/dashboard real
  implementation (circuit break + session termination + dashboards)
  under the same discipline: spec-first, fresh-context review, live
  e2e before release.
